This is a simple Flask Webapp to Demonstrate the Process of hiding a text data inside a Image using LSB algorithm. the below Packages are required to run this Program.
pip install Flask
pip install opencv-python
